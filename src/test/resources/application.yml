spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

  datasource:
    driver-class-name:  org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    password: sa
    username: sa
  flyway:
    enabled:  false
